<!DOCTYPE html>
<html>
    <head>
        <title>Blog for news resource - Appcake test(Senior Backend engineer)</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
    </head>
    <body>
        <div>
        <div>
            <div class="row">
                <div class="col-auto me-auto"></div>
                <div class="col-auto"><button type="button" class="btn btn-link"><a href='http://localhost:8000/login'>logout</a></button></div>
            </div>
        </div>
            <div class="row row-cols-1 row-cols-sm-2" style="margin: 20px;">
            {% for article in articles %}
                
                    <div class="card mb-3" style="max-width: 540px; margin: 10px;">
                        <div class="row g-0">
                        <div class="col-md-4">
                            <img src="{{ article.picture }}" class="img-fluid rounded-start" alt="...">
                        </div>
                        <div class="col-md-8">
                            <div class="card-body">
                            <h5 class="card-title">{{article.title}}</h5>
                            <p class="card-text">{{article.description}}</p>
                            <p class="card-text"><small class="text-muted">{{article.date}}</small> {% if is_granted('ROLE_ADMIN') %} <button type="button" class="btn btn-link" ><a href="?page_no={{page_no}}&&del={{article.title}}">Delete</a></button> {% endif %}</p>
                            </div>
                        </div>
                        </div>
                    </div>                           
                
            {% endfor %}
            </div>
        </div>
        <div style="margin: 20px;">
            <nav aria-label="Page navigation example">
                <ul class="pagination">
                  <li class="page-item {% if page_no == 0 %} disabled {% endif %}"><a class="page-link" href="{% if page_no > 1 %} ?page_no={{previous_page}} {% endif %}">Previous</a></li>                    
                    {% for i,l in 0.. count %}
                        <li class="page-item {% if page_no == l %} active {% endif %}"><a class="page-link" href="?page_no={{i}}">{{ i }}</a></li>
                    {% endfor %}
                  <li class="page-item {% if page_no == count %} disabled {% endif %}""><a class="page-link" href="?page_no={{next_page}}">Next</a></li>
                </ul>
            </nav>
        </div>
    </body>
</html>
